import AbstractComponent from '@components/abstract';

const Title = () => {
    return (
        <AbstractComponent>
            <div>사역 타이틀 페이지</div>
        </AbstractComponent>
    );
};

export default Title;
