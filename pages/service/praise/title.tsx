import AbstractComponent from '@components/abstract';

const Title = () => {
    return (
        <AbstractComponent>
            <div>찬양 타이틀 페이지</div>
        </AbstractComponent>
    );
};

export default Title;
